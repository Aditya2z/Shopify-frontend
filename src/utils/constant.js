const RootUrl = " https://shopify-app-i-5154034830ba.herokuapp.com/api";

const productUrl = `${RootUrl}/products`;
const usersUrl = `${RootUrl}/users`;
const userVerifyUrl = `${RootUrl}/users/user`;
const cartUrl = `${RootUrl}/cart`;

const localStorageKey = "e-commerce_user";

export {
  RootUrl, productUrl, usersUrl, userVerifyUrl, localStorageKey, cartUrl,
};
